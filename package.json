{
  "name": "tarefas-backend",
  "version": "1.0.0",
  "description": "API para gerenciamento de tarefas com Prisma e MongoDB",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"cd backend && nodemon server.js\" \"cd frontend && live-server --port=5500 --open=index.html\""
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "live-server": "^1.2.2",
    "nodemon": "^3.1.10",
    "prisma": "^6.18.0"
  }
}
